# PWA V0.1 — Samsung Galaxy S23 验收记录

更新日期：2026-07-24

## 验收对象

- 当前开发主线：`pwa/`（React + TypeScript + Vite PWA）
- 目标设备：Samsung Galaxy S23
- 建议测试浏览器：最新版 Samsung Internet、最新版 Chrome
- S23 基准视口：360 × 780 CSS 像素
- 基线提交：`e366c39 chore: capture current project baseline`

## 已自动完成

| 项目 | 结果 | 证据 |
|---|---|---|
| 单元测试 | 通过 | 3 个测试文件、9 个测试全部通过 |
| 生产构建 | 通过 | TypeScript 检查、Vite 构建和 PWA Service Worker 生成成功 |
| 首页响应式 | 通过 | 360 × 780 下无横向溢出，控制台无应用错误 |
| 训练页响应式 | 通过 | 360 × 780 下内容完整占满一屏，无横向溢出 |
| 完整训练流程 | 通过 | 可完成 3 组并进入 SUCCESS |
| 结果记录 | 通过 | 完成后显示经验 +10、连续训练 1 天 |
| 用户反馈 | 通过 | “适中”反馈可保存并显示在历史记录 |
| 历史页响应式 | 通过 | 完成次数、连续天数、经验正确 |

自动化浏览器使用独立测试存储，不会修改手机上的训练记录。

## 当前无法代替真机确认

运行 `adb devices -l` 时没有发现已授权的 Android 设备，因此以下项目需要连接 S23 后人工确认：

- Web Vibration API 在 Chrome / Samsung Internet 中的实际触觉强度
- 挖孔、状态栏和底部手势区的安全区表现
- 添加到主屏幕、独立窗口启动和图标显示
- 离线冷启动
- 锁屏、切到后台后返回时的计时行为
- 120 Hz 屏幕上的动画流畅度和耗电表现

## S23 测试连接方法

推荐使用 USB + ADB 端口转发：

1. 手机开启“开发者选项”和“USB 调试”。
2. 用支持数据传输的 USB 线连接电脑。
3. 在手机弹窗中允许这台电脑进行 USB 调试。
4. 在电脑执行：

```powershell
adb devices -l
adb reverse tcp:4173 tcp:4173
```

5. 在项目的 `pwa` 目录启动：

```powershell
npm run dev -- --host 127.0.0.1 --port 4173
```

6. 手机浏览器打开 `http://127.0.0.1:4173/`。

该方法不依赖局域网 Network 地址。

## 真机审查清单

### P0：核心流程

- [ ] 首页面板完整，不被顶部挖孔或底部手势条遮挡
- [ ] 点击“开始训练”后出现准备倒计时
- [ ] 顺序为 READY → CONTRACT → HOLD → RELAX
- [ ] CONTRACT 5 秒、HOLD 3 秒、RELAX 5 秒
- [ ] 连续完成 3 组后只进入一次完成页
- [ ] 结果为经验 +10、连续训练天数正确
- [ ] 选择反馈后，历史记录中显示同一反馈
- [ ] 刷新页面后历史记录仍存在

### P1：移动体验

- [ ] 动画位于主要视线中心，提示和倒计时不影响动画观察
- [ ] 全程没有横向滚动、文字重叠或按钮无法点击
- [ ] HOLD 和 RELAX 的触觉反馈能感觉到，且不会连续振动
- [ ] 120 Hz 模式下动画肉眼流畅
- [ ] 系统字体调大一档后仍可完成训练
- [ ] 手势导航和三键导航下底部内容均可操作

### 已知行为专项

- [ ] CONTRACT 阶段切到后台 5 秒后返回，记录当前显示状态
- [ ] HOLD 阶段锁屏 20 秒后返回，记录是否跳过阶段或直接完成
- [ ] 训练中点击左上角返回，记录是否会直接退出

当前代码按照真实经过时间推进训练。后台或锁屏返回时可能跳过阶段；训练中返回目前也没有确认弹窗。这两项是下一阶段的核心稳定性候选。

## PWA 安装说明

USB 转发的 HTTP 页面适合功能和布局调试，但 Android 上完整的 PWA 安装、Service Worker 与离线能力应使用 HTTPS 地址验证。

当前 Manifest 只配置了 SVG 图标。Chrome 通常可以读取，但正式 Android 安装验收前建议补充 192 × 192、512 × 512 PNG 和 maskable 图标。

## 缺陷记录模板

```text
编号：
优先级：P0 / P1 / P2 / P3
设备与系统：Samsung Galaxy S23 / Android ______
浏览器与版本：
复现步骤：
实际结果：
预期结果：
复现概率：
附件：截图 / 录屏
```

